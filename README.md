---
# ✅ Structure Rules

1. Always use a main container to wrap the layout.
2. Use semantic HTML (`main`, `nav`, `article`, `aside`).
3. A single tweet should be an `article`.
4. Class names should describe purpose, not appearance.
---

# ✅ Accessibility & Buttons Rules

5. Anything clickable must be a `button` or `a`.
6. Navigation tabs (For You / Following) must be buttons.
7. Icons that perform actions must be buttons.
8. Like, Comment, Retweet must be buttons.
9. Trending items must be focusable (link or button).
10. All interactive elements must be keyboard accessible.

---

# ✅ Forms Rules

11. Textarea must have a proper placeholder.
12. Form elements should be accessible and clear.

---

# ✅ Images Rules

13. Always use meaningful `alt` text.
14. Decorative images should have empty `alt=""`.

---

# ✅ CSS Rules

15. Avoid inline styles.
16. Use reusable utility classes.
17. Use CSS variables for repeating colors.
18. Keep spacing, font sizes, and weights consistent (design system).
19. Don’t create unnecessary class names.
20. Keep CSS clean and reusable.

---
